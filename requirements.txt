streamlit>=1.32.0,<2.0
pypdf>=4.0.0
sentence-transformers>=2.7.0
numpy>=1.26.0,<2.0
requests>=2.31.0
gTTS>=2.5.0

# LLM providers (all three — user picks in sidebar)
groq>=0.11.0
anthropic>=0.40.0
openai>=1.40.0

# Torch is pulled by sentence-transformers; we let pip resolve the CPU build
# so the same requirements file works on Streamlit Cloud + HF Spaces.
